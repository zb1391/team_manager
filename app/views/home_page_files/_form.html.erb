<%= form_for @home_page_file, :html => { :multipart => true } do |f| %>
  <% if @home_page_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@home_page_file.errors.count, "error") %> prohibited this home_page_file from being saved:</h2>

      <ul>
      <% @home_page_file.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :the_file %><br>
    <%= f.file_field :the_file %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
